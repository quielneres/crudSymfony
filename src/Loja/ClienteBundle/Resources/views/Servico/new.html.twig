{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Servico creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('servico') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
