{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Recibo creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('recibo') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
